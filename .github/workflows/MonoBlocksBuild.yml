name: MonoBlocks Build & Pre-Release

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ----------------------------
      # Java 8 required for 1.7.10 ForgeGradle
      # ----------------------------
      - name: Setup Java 8
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 8

      # ----------------------------
      # Gradle setup
      # ----------------------------
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          validate-wrappers: false

      # ----------------------------
      # Cache Gradle
      # ----------------------------
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Make gradlew executable
        run: chmod +x gradlew

      # ----------------------------
      # Run unit tests explicitly
      # ----------------------------
      - name: Run Unit Tests
        run: ./gradlew test --stacktrace

      # ----------------------------
      # Build mod
      # ----------------------------
      - name: Gradle Build
        run: ./gradlew build --stacktrace

      # ----------------------------
      # Extract Gradle version
      # ----------------------------
      - name: Extract project version
        id: version
        run: |
          VERSION=$(./gradlew -q properties | awk -F': ' '/^version:/ {print $2; exit}')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      # ----------------------------
      # Check if release already exists
      # ----------------------------
      - name: Check if release already exists
        id: check_release
        run: |
          git fetch --tags
          if git tag | grep -q "v${{ steps.version.outputs.version }}"; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Release v${{ steps.version.outputs.version }} already exists — skipping publish"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "No existing release — will publish"
          fi

      # ----------------------------
      # Create GitHub Pre-Release
      # ----------------------------
      - name: Create GitHub Pre-Release
        if: steps.check_release.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: MonoBlocks ${{ steps.version.outputs.version }}
          prerelease: true
          body: |
            Automated release for **MonoBlocks**

            Branch: `main`
            Version: `${{ steps.version.outputs.version }}`
            Commit: `${{ github.sha }}`

            ⚠ **Note:** If this is a pre-release. It is intended for testing and **does not guarantee stability or completeness**.
          files: |
            build/libs/monoblocks-${{ steps.version.outputs.version }}*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}